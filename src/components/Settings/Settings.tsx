import SettingsIcon from "../../assets/Icons/SettingsIcon";

export default function Settings() {
    return (
        <button id='settings' className="fixed top-2 right-2">
            <SettingsIcon />
        </button>
    )
}